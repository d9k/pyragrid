###
# app configuration
# http://docs.pylonsproject.org/projects/pyramid/en/1.5-branch/narr/environment.html
###

[app:main]
use = egg:best_tests_server

pyramid.reload_templates = true
pyramid.debug_authorization = false
pyramid.debug_notfound = false
pyramid.debug_routematch = false
pyramid.default_locale_name = ru
pyramid.includes =
    pyramid_debugtoolbar
    pyramid_tm
    pyramid_beaker

;sqlalchemy.url = sqlite:///%(here)s/best_tests_server.sqlite
sqlalchemy.url = postgresql://d9k:azaz15@localhost/BestTests

# By default, the toolbar only appears for clients from IP addresses
# '127.0.0.1' and '::1'.
# debugtoolbar.hosts = 127.0.0.1 ::1

# beaker settings ( http://docs.pylonsproject.org/projects/pyramid-beaker/en/latest/)

# http://beaker.readthedocs.org/en/latest/configuration.html#session-options )
session.type = file
session.data_dir = %(here)s/data/session/data
session.lock_dir = %(here)s/data/session/lock
# Name of the cookie key used to save the session under
session.key = best_tests
session.secret = trogk6ry
session.cookie_on_exception = true

# beaker cache settings
# http://beaker.readthedocs.org/en/latest/configuration.html#cache-options
#cache.regions = default_term, second, short_term, long_term
#cache.type = memory
#cache.second.expire = 1
#cache.short_term.expire = 60
#cache.default_term.expire = 300
#cache.long_term.expire = 3600

###
# wsgi server configuration
###

[server:main]
use = egg:waitress#main
host = 0.0.0.0
port = 6543

###
# logging configuration
# http://docs.pylonsproject.org/projects/pyramid/en/1.5-branch/narr/logging.html
###

[loggers]
keys = root, best_tests_server, sqlalchemy

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = console

[logger_best_tests_server]
level = DEBUG
handlers =
qualname = best_tests_server

[logger_sqlalchemy]
level = INFO
handlers =
qualname = sqlalchemy.engine
# "level = INFO" logs SQL queries.
# "level = DEBUG" logs SQL queries and results.
# "level = WARN" logs neither.  (Recommended for production systems.)

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s][%(threadName)s] %(message)s
